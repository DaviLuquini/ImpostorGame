export enum GamePhase {
    Setup = 'setup',
    Lobby = 'lobby',
    RoleReveal = 'role-reveal',
    Discussion = 'discussion',
    Voting = 'voting',
    VoteResults = 'vote-results',
    Results = 'results'
}
